<div class="haven-conversation" data-controller="chat" data-chat-closed-value="<%= @chat.closed? %>">
  <header class="conversation-header">
    <%= link_to chats_path, class: "back-btn" do %>
      <i class="fa-solid fa-arrow-left"></i>
    <% end %>
    <div class="header-title-section">
      <h1><%= @chat.display_title %></h1>
      <% if @chat.closed? %>
        <span class="badge-closed">Terminée</span>
      <% end %>
    </div>
    <% unless @chat.closed? %>
      <button type="button" class="finish-btn" data-action="click->chat#confirmClose" data-chat-url="<%= close_chat_path(@chat) %>">
        <i class="fa-solid fa-check"></i>
        Terminer
      </button>
    <% else %>
      <div class="header-spacer"></div>
    <% end %>
  </header>

  <div class="conversation-messages" id="messages" data-chat-target="messages">
    <% if @messages.empty? %>
      <div class="chat-empty">
        <p>Commence à discuter avec Haven...</p>
      </div>
    <% else %>
      <% @messages.each do |message| %>
        <div class="message-bubble <%= message.role %>">
          <div class="message-sender">
            <%= message.role == 'user' ? 'Vous' : 'Haven (IA)' %>
          </div>
          <div class="message-text">
            <%= message.content %>
          </div>
          <div class="message-time">
            <%= message.created_at.strftime("%d %b %H:%M") %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>

  <% unless @chat.closed? %>
    <div class="conversation-input" data-controller="speech" data-speech-auto-submit-value="false">
      <%= form_with url: chat_messages_path(@chat), method: :post, local: true, class: "message-form", data: { chat_target: "form", speech_target: "form", action: "submit->chat#handleSubmit" } do |f| %>
        <%= f.text_area :content,
            name: "message[content]",
            placeholder: "Écrivez votre message...",
            class: "message-textarea",
            rows: 1,
            required: true,
            data: { chat_target: "textarea", speech_target: "input", action: "keydown->chat#handleKeydown input->chat#autoResize" } %>
        <button type="button" class="mic-btn" data-speech-target="button" data-action="click->speech#toggle">
          <i class="fa-solid fa-microphone"></i>
        </button>
        <button type="submit" class="send-btn" data-chat-target="submitBtn">
          <i class="fa-solid fa-paper-plane"></i>
        </button>
      <% end %>
    </div>
  <% else %>
    <div class="conversation-closed-notice">
      <i class="fa-solid fa-lock"></i>
      Cette conversation est terminée
    </div>
  <% end %>

  <!-- Modal de confirmation -->
  <div class="confirm-modal" id="confirm-modal" data-chat-target="confirmModal">
    <div class="confirm-modal-content">
      <div class="confirm-modal-icon" data-chat-target="modalIcon">
        <i class="fa-solid fa-check-circle"></i>
      </div>
      <h2 data-chat-target="modalTitle">Terminer cette conversation ?</h2>
      <p data-chat-target="modalText">Tu pourras toujours la consulter, mais tu ne pourras plus envoyer de messages.</p>
      <div class="confirm-modal-actions">
        <button type="button" class="btn-cancel" data-action="click->chat#cancelClose">Annuler</button>
        <button type="button" class="btn-confirm" data-action="click->chat#executeClose">Confirmer</button>
      </div>
    </div>
  </div>
</div>

<style>
/* Stoic Warm Light Theme - VERSION DEMO-DAY: Contrastes renforcés TV */
.haven-conversation {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #E8E2D9;
  color: #2A2622;
}

.conversation-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 1.5rem;
  border-bottom: 2px solid #C4B8A8;
  gap: 1rem;
  background: #F5F2EB;
}

.header-title-section {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  min-width: 0;
}

.conversation-header h1 {
  font-size: 1.1rem;
  font-weight: 600;
  margin: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  color: #2A2622;
}

.badge-closed {
  background: rgba(160, 111, 46, 0.2);
  color: #8A5F28;
  padding: 0.25rem 0.75rem;
  border-radius: 1rem;
  font-size: 0.75rem;
  font-weight: 600;
  white-space: nowrap;
}

.back-btn {
  background: none;
  border: none;
  color: #524C44;
  font-size: 1.1rem;
  cursor: pointer;
  opacity: 0.8;
  transition: opacity 0.2s;
  text-decoration: none;
  padding: 0.5rem;
}

.back-btn:hover {
  opacity: 1;
  color: #2A2622;
}

.header-spacer {
  width: 80px;
}

.finish-btn {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background: #A06F2E;
  color: #F5F2EB;
  border: none;
  border-radius: 0.5rem;
  font-size: 0.9rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
}

.finish-btn:hover {
  background: #8A5F28;
}

.finish-btn i {
  font-size: 0.85rem;
}

.conversation-messages {
  flex: 1;
  padding: 1.5rem;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 1rem;
  scroll-behavior: smooth;
}

.chat-empty {
  text-align: center;
  color: #7A7368;
  margin-top: 2rem;
}

.message-bubble {
  max-width: 80%;
  padding: 0.875rem 1rem;
  border-radius: 1rem;
  position: relative;
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.message-bubble.user {
  align-self: flex-end;
  background: #DDD7CC;
  border-bottom-right-radius: 0.25rem;
  color: #2A2622;
}

.message-bubble.assistant {
  align-self: flex-start;
  background: #F5F2EB;
  border: 2px solid #C4B8A8;
  border-bottom-left-radius: 0.25rem;
  color: #2A2622;
}

.message-sender {
  font-size: 0.7rem;
  font-weight: 700;
  margin-bottom: 0.35rem;
  color: #A06F2E;
}

.message-text {
  font-size: 0.95rem;
  line-height: 1.5;
  white-space: pre-wrap;
  color: #2A2622;
}

.message-time {
  font-size: 0.65rem;
  color: #7A7368;
  margin-top: 0.35rem;
  text-align: right;
}

/* Curseur clignotant pour le streaming */
.typing-indicator {
  display: inline-block;
  width: 8px;
  height: 16px;
  background: #C4923A;
  margin-left: 2px;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}

.conversation-input {
  padding: 1rem 1.5rem;
  padding-bottom: calc(1rem + env(safe-area-inset-bottom, 0));
  background: #F5F2EB;
  border-top: 2px solid #C4B8A8;
}

.message-form {
  display: flex;
  align-items: flex-end;
  gap: 0.75rem;
}

.message-textarea {
  flex: 1;
  padding: 0.75rem 1rem;
  border: 2px solid #C4B8A8;
  border-radius: 1.5rem;
  background: #F5F2EB;
  color: #2A2622;
  font-size: 0.95rem;
  resize: none;
  outline: none;
  max-height: 120px;
  line-height: 1.4;
  overflow-y: auto;
}

.message-textarea::placeholder {
  color: #7A7368;
}

.message-textarea:focus {
  border-color: #A06F2E;
  background: #F5F2EB;
}

.mic-btn {
  width: 44px;
  height: 44px;
  padding: 0;
  background: #DDD7CC;
  color: #524C44;
  border: none;
  border-radius: 50%;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.mic-btn:hover {
  background: #C4B8A8;
  color: #2A2622;
}

.mic-btn.listening {
  background: #A06F2E;
  color: #F5F2EB;
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.send-btn {
  width: 44px;
  height: 44px;
  padding: 0;
  background: #A06F2E;
  color: #F5F2EB;
  border: none;
  border-radius: 50%;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.send-btn:hover {
  background: #8A5F28;
}

.send-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.conversation-closed-notice {
  padding: 1rem 1.5rem;
  padding-bottom: calc(1rem + env(safe-area-inset-bottom, 0));
  background: #F5F2EB;
  border-top: 2px solid #C4B8A8;
  text-align: center;
  color: #7A7368;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

/* Modal de confirmation */
.confirm-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(42, 38, 34, 0.5);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  padding: 1rem;
}

.confirm-modal.active {
  display: flex;
}

.confirm-modal-content {
  background: #F5F2EB;
  border: 2px solid #C4B8A8;
  border-radius: 1rem;
  padding: 2rem;
  max-width: 400px;
  width: 100%;
  text-align: center;
  animation: modalSlideIn 0.3s ease;
}

@keyframes modalSlideIn {
  from {
    opacity: 0;
    transform: scale(0.9) translateY(20px);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

.confirm-modal-icon {
  width: 60px;
  height: 60px;
  background: rgba(160, 111, 46, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1rem;
}

.confirm-modal-icon i {
  font-size: 1.75rem;
  color: #C4923A;
}

.confirm-modal-content h2 {
  color: #2A2622;
  font-size: 1.25rem;
  margin: 0 0 0.5rem;
}

.confirm-modal-content p {
  color: #524C44;
  font-size: 0.9rem;
  margin: 0 0 1.5rem;
  line-height: 1.5;
}

.confirm-modal-actions {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
}

.btn-cancel {
  padding: 0.75rem 1.5rem;
  background: #DDD7CC;
  color: #524C44;
  border: 2px solid #C4B8A8;
  border-radius: 0.5rem;
  font-size: 0.9rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-cancel:hover {
  background: #C4B8A8;
  color: #2A2622;
}

.btn-confirm {
  padding: 0.75rem 1.5rem;
  background: #A06F2E;
  color: #F5F2EB;
  border: none;
  border-radius: 0.5rem;
  font-size: 0.9rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-confirm:hover {
  background: #8A5F28;
}

/* Responsive adjustments */
@media (max-width: 480px) {
  .conversation-header {
    padding: 0.875rem 1rem;
  }

  .conversation-header h1 {
    font-size: 1rem;
  }

  .finish-btn {
    padding: 0.4rem 0.75rem;
    font-size: 0.85rem;
  }

  .conversation-messages {
    padding: 1rem;
  }

  .message-bubble {
    max-width: 85%;
  }
}
</style>
